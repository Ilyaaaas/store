import { StyleSheet } from 'react-native';

export const inputStyle = StyleSheet.create({
  inputItem: {
    borderRadius: 8,
    overflow: 'hidden',
    backgroundColor: '#a2a3b7',
    marginTop: 8,
  },
});
